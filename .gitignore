# Left off for now per request - large file, add back later if wanted
Videos/07-Bellafill Nonsurgical Brow Lift.mov

# OS junk
Thumbs.db
.DS_Store

# Local secrets (Square access token, etc.)
.env
server/.env
